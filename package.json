{
  "name": "dsa",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest ts",
    "test:cov": "vitest ts --coverage --coverage.exclude=\"**/*start*\" --coverage.exclude=\"**/common\"",
    "test:debug": "vitest ts --run --inspectBrk --no-file-parallelism",
    "test:ui": "vitest ts --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.3.1",
    "@vitest/ui": "^1.3.1",
    "vitest": "^1.3.1"
  }
}
